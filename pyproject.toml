[project]
name = "fastvlm-coreai"
version = "0.1.0"
description = "FastVLM to Core AI .aimodel conversion pipeline"
requires-python = ">=3.11"
dependencies = [
    "coreai-torch",
    "coreai-core",
    "coreai-opt",
    "safetensors",
    "huggingface-hub",
    "numpy",
    "timm",
    "transformers",
]

[tool.uv]
prerelease = "allow"

[tool.setuptools]
packages = []

[dependency-groups]
dev = []
