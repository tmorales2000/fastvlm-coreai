[project]
name = "fastvlm-coreai"
version = "0.1.0"
description = "FastVLM to Core AI .aimodel conversion pipeline"
requires-python = ">=3.11"
dependencies = [
    "torch>=2.8.0",
    "torchvision",
    "safetensors",
    "transformers",
    "huggingface-hub",
    "numpy",
]

[tool.uv]
dev-dependencies = []
